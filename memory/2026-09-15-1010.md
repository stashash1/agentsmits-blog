# Session: 2026-09-15 10:10:xx Europe/Moscow (continuation)

## TL;DR
**Telegram снова живой.** За последние ~25 мин: 7 постов ушли в канал (msg_id 1617+),
pending упал 144 → 136, "no analysis" блокер снят.

## Что сделано (по фазам)

### Phase A — Drain + first publish
- ✅ `_heuristic_analyze.py` → заполнил 43 items (шаблонные RU impact, EN title)
- ✅ Test publish `--limit 5` → **4 items отправлены**, failed=0, elapsed=3s
- ✅ Telegram TCP 149.154.167.99:443 — OK (сеть вернулась с 08.09)
- ✅ AmneziaVPN-service Running (Automatic)
- ✅ Ollama жив, 14 моделей (qwen3:8b/14b, deepseek-r1:14b/32b, gemma4:26b/31b)

### Phase A2 — LLM upgrade
- ⚠️ `_llm_analyze.py --limit 50` запустил, qwen3:8b дал ~50% JSON parse failures (arxiv long summaries)
- На 24/47 остановил (≈25 мин потрачено, throughput ~60-90s/item)
- Оставшиеся 23 items оставлены для cron-цикла с лимитом 8

### Phase B — Schedule analyze
- ❌ `Register-ScheduledTask` — Access denied (нет full admin в shell)
- ❌ `schtasks /create /ru SYSTEM` — тоже Access denied
- ✅ **Fallback:** встроил analyze-шаг прямо в `cron_publish.ps1` (pre-step)
- ✅ Добавил `--limit` arg в `_llm_analyze.py` (default 50, в cron передаю 8)
- ✅ Verify end-to-end: `analyze --limit 3 + publish --limit 3` → py_compile OK, --help OK, 3 published

### Verify Phase B
```
py_compile: OK
_llm_analyze.py --help: показывает --limit
analyze --limit 3: 1 success / 2 fail (qwen3:8b JSON inconsistency), 23s/item
publish --limit 3: 3 published, 0 failed
pending: 144 → 136, ru_titles=107, en_titles=29
published ever: 1070
```

## Состояние системы на 15.09 10:10

| Метрика | Значение | Δ за сегодня |
|---|---|---|
| pending | 136 | -8 (4 + 3 тест + 1 LLМ) |
| ru-title pending | 107 | +newly LLM-analyzed |
| en-title pending | 29 | оставлены для следующих циклов |
| published (today) | 7 (msg_id 1617+) | впервые после 06.09 |
| Telegram TCP | OK | было timeout с 08.09 |
| Ollama | OK | 14 models |
| AmneziaVPN daemon | Running | без изменений |

## Что осталось (не блокирует работу)

1. **`agentsblog-analyze` scheduled task** — Register-ScheduledTask нужен admin shell.
   Workaround (встроенный в cron_publish.ps1) уже работает.
   Чтобы починить архитектурно: запустить `register_analyze.ps1` из elevated PowerShell.

2. **`agentsblog-daily-summary`** (из прошлой сессии) — нужен от пользователя:
   - bot token (через secrets → файл)
   - numeric chat_id `@AgentSmit_sync`
   - старая scheduled task может всё ещё указывать на удалённый `pipeline/daily_summary.py`

3. **Удалить старую задачу `agentsblog-daily-summary`** — она сейчас exit 127 (старый путь).
   Команда: `schtasks /Delete /TN "agentsblog-daily-summary" /F`

4. **Закоммитить правки** (uncommitted):
   - `scripts/_llm_analyze.py` (+argparse --limit)
   - `scripts/cron_publish.ps1` (+analyze pre-step)
   - `scripts/cron_analyze.ps1` (новый, на случай если удастся зарегистрировать task)
   - diag-скрипты в `_diag/`

## Уроки (для MEMORY.md)

- **qwen3:8b нестабилен на JSON** для длинных arxiv-саммари (~50% failures).
  В cron лучше лимит 8, не 50. Альтернатива — qwen3:14b (есть в Ollama), должна быть стабильнее.
- **Task Scheduler / schtasks SYSTEM** тоже требует admin elevation в shell.
  Если не получается — fallback через встраивание в существующий cron-цикл.
- **heuristic_analyze — критический fallback.** Без LLM всё равно публикуемо, пусть и с EN-titles.
  Не блокирует pipeline.

## Команды для пользователя (копипаст)

### Удалить старую broken-задачу (exit 127):
```powershell
schtasks /Delete /TN "agentsblog-daily-summary" /F
```

### Из admin PowerShell — зарегистрировать analyze:
```powershell
cd C:\Users\Admin\dev\project\agentsmits-blog
powershell -NoProfile -ExecutionPolicy Bypass -File _diag\register_analyze.ps1
```

### Если хочется лучшее качество LLM (qwen3:14b вместо 8b):
```powershell
$env:OLLAMA_MODEL = 'qwen3:14b'
# или прописать в $env:AGENTSBLOG_ENV
```

## Логи этой сессии
- `_diag/phase_a1.ps1` — heuristic drain (43 items)
- `_diag/ollama_check.ps1` — health check (Ollama+VPN+TCP)
- `_diag/phase_a2.ps1` — LLM analyze (killed at 24/47)
- `_diag/phase_a3_publish.ps1` — test publish (4 sent)
- `_diag/verify_publish.ps1` — verify state after publish
- `_diag/try_schtasks.ps1` — schtasks attempt (denied)
- `_diag/verify_phase_b.ps1` — final fallback verification (3 published)
