<!-- project: github.com/stashash1/agentsmits-blog -->

## 2026-09-14: Daily summary automation recovery

- Реализован `agentsblog daily-summary` CLI-команда с поддержкой `--date` и dry-run
- Созданы launchers: `daily_summary.ps1` и `daily_summary.sh`
- Документация: `scripts/daily_summary.README.md` с env-требованиями
- Верифицировано: форматирование digest-сообщения (25 статей → 1362 символов)
- Блокеры: требуется bot.token и numeric chat_id для Telegram доставки
- Старая задача `agentsblog-daily-summary` требует удаления перед регистрацией новой

## 2026-09-14 14:00–14:17: Pipeline recovery dry-run

**Запрос:** «что там с публикацией новостей» — канал молчит 4 дня (с 10.09).

### Воспроизведённые блокеры

1. **`agent_impact` пустой** — главный. Publisher (`models.py:131-134`) скипает всё, где `not bool(agent_impact)`.
   - Поле заполняет `scripts/_llm_analyze.py` через локальный Ollama (`qwen3:8b`, 5.2 ГБ, timeout 120s/call).
   - Ollama живой, модель стоит. Мой probe (`Reply with one word: ok`, num_predict=10): **3 секунды** end-to-end (2.85с — load, 0.12с — eval).
   - Последний analyze-run (12.09 02:15) провалил 0/5 за 600.1s/item — холодный старт / нагрузка ночью.
   - **Analyze НЕ входит в cron-cluster** (`15 */6 * * *` — это scan+cluster). Получается, между cluster и publish всегда был ручной запуск `_llm_analyze.py`.
   - **Fix применён:** `scripts/_heuristic_analyze.py` заполнил 5 items с пустым `agent_impact` шаблонами по категориям. У остальных 109 items `agent_impact` уже был (видимо, миграция / старые runs). После heuristic `publish --dry-run` дошёл до стадии отправки.

2. **Telegram сеть лежит** — оба `getMe` и `getChat` таймаутят 10s.
   - DNS ✅ `api.telegram.org → 149.154.167.99`
   - Hosts ✅ `149.154.167.99 api.telegram.org` на месте
   - `AmneziaVPN-service` ✅ запущен (PID 5252)
   - TCP до `149.154.167.99` ❌ timeout (прямой IP, через домен, urllib + Invoke-WebRequest)
   - **Вывод:** демон VPN жив, но туннель не поднят (или РКН режет IP). Нужно reconnect клиента.
   - Token валидный (46 chars, prefix `8743212027`).

### Operational findings

- 17/39 sources никогда не сканировались (legacy-записи в DB, hyphen-варианты имён). Сканер активен только на 22.
- Python в PATH не было; работает `C:\Users\Admin\python312\python.exe` (есть второй в `C:\Users\Admin\AppData\Local\Programs\Python\Python312\python.exe`).
- PowerShell-консоль CP866, mojibake в выводе. Лечить: `[Console]::OutputEncoding = UTF8; chcp 65001; $env:PYTHONIOENCODING='utf-8'` в начале каждой команды.
- `openclaw cron list`: `agentsmits-blog-cluster` (15 */6 *) ok, `agentsmits-blog: dail...` (0 21 *) error (7x), `Brent research` (0 3 *) error.

### Pending

- Жду подтверждения переподключения VPN → re-test `getMe` → если ok, `publish --limit 1 --allow-during-quiet` → если ok, остальные по слотам (T1→T2→T3 digest).
- Бэкграундом запустить `_llm_analyze.py` (Ollama горячий, ~5-10s/item, всего 10-20 мин на 114 items) — повысит качество поверх heuristic-шаблонов.
- Daily 21:00 cron рассосётся после починки сети.
- 17 неактивных sources — отдельная задача, не блокер.