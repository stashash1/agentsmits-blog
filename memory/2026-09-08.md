# 2026-09-08

## 00:15 MSK — agentsmits-blog-cluster cron run

**6-hour cycle window:** 00:15 → 06:15 MSK (Tue 2026-09-08).
**Quiet hours:** 23:00–08:00 MSK — **ACTIVE** (00:15 in window → ALL slots deferred).

**Pipeline state (re-checked against `data/blog.db`):**
- Pending in DB: **41** items (all analyzed; legacy `data/pending_queue.json` is stale — 6 items, last touch 2026-09-06 04:05; current publisher reads DB)
- Published (last 24h): 25; latest msg_id=1552 (huggingface-real-time-intelligence) at 12:29 MSK 2026-09-07
- AGI counter: base_days=1460, current_days=729 (~50%); `compute_agi()` returns 1210/17%
- DB events table last entry: 2026-09-07T21:09:24 UTC (publish completed, skipped=4)
- 4 publishable items after `list_pending(min_importance=3)`; 37 below threshold

**Publishable items (imp ≥ 3) — full state:**

| ID | Source | Date | Imp | Decayed | Title (RU) | Narr |
|---|---|---|---|---|---|---|
| arxiv-2609.05339 | arxiv | 2026-09-04 | 4 | 3.87 | Выживет ли память агента после обновления модели? | None |
| google_ai-fairwind-program | google_ai | 2026-09-02 | 4 | 3.80 | Противодействие киберугрозам: защита правительств и корпораций | None |
| arxiv-2609.05395 | arxiv | 2026-09-04 | 4 | 3.87 | Многоступенчатый вызов инструментов через открытые API КНДР | None |
| cursor-self-hosted-machines | cursor | 2026-09-07 | 3 | 2.97 | Самохозяйственные серверы | None |

**Tier classification + slot schedule:**

| Tier | Slot (MSK) | Item | Why |
|---|---|---|---|
| **Tier 1** | **08:05** | `arxiv-2609.05339` | Agent memory portability — critical pain point, fresh (4d), standard post |
| **Tier 1** | **08:35** | `google_ai-fairwind-program` | Tier-1 source (Google AI), safety/security, slightly older (6d) |
| **Tier 2** | **09:05** | `arxiv-2609.05395` | Korean API tool-calling benchmark — niche but high-imp (data sovereignty angle) |
| **Tier 2** | **09:35** | `cursor-self-hosted-machines` | Fresh product update (today), Tier-2 source, dev-relevant |
| Tier 3 (QW4-skip) | — | 37× imp<3 items | Filtered by `list_pending(min_importance=3)` BEFORE release grouping |

**Note:** All 4 scheduled slots fall AFTER quiet hours end (08:00 MSK). The 00:15-06:15 cycle window has zero publishable slots — everything defers to the next non-quiet window starting 08:00 MSK Tue. Publisher auto-defer verified via dry-run: returns `quiet_deferred: 4` with `published=0`.

**Analyst notes per item:**

1. **`arxiv-2609.05339` (Tier 1) — Memory survives model upgrade** — `decayed_importance=3.87`, age=4d, half-life decay=4d. arxiv = tier-3 source (penalty 0.85×), but `importance=4` from `agent/memory/upgrade` keyword hits. No breakthrough (Russian title doesn't match ARCHITECTURE_PATTERNS — pure benchmark study, not a new architecture). Standard post format. Reasoning: this is the most impactful for the broad agent-developer audience; addresses known memory-loss pain point. Dedup: clean. Narrative: None → will create new narrative on publish. AGI counter impact if published: −1d (imp=4 rule).

2. **`google_ai-fairwind-program` (Tier 1) — Fairwind cyber defense** — Google AI = tier-1 source, but `importance=4` came only from `importance_boost` and `safety/security` keyword matches (no architecture/sota). Age=6d (half-life decay kicks in but not critical). Strategic value: government/enterprise access to cyber-defense tooling — niche but high-value audience. Standard post format. Dedup: clean. AGI counter impact: −1d.

3. **`arxiv-2609.05395` (Tier 2) — Korean API tool-calling** — Similar scoring profile to #1 but narrower audience (Korean public APIs, data sovereignty theme). Tier 2 instead of Tier 1 because the topic is geographically scoped — broader community won't act on it directly. Standard post. Dedup: clean.

4. **`cursor-self-hosted-machines` (Tier 2) — Cursor self-hosted** — Fresh (today, age=1d, decayed=2.97). Cursor = tier-2 source. Minimal analysis body — title is "Self-hosted machines", summary is 1 sentence. Importance 3 (no high-impact keywords, no breakthrough signals). Tier 2 because Cursor is widely used by our readership; dev privacy/compliance angle. Standard post. Dedup: clean.

**Breakthrough check (all 4 items):** 0/4 breakthroughs detected. `detect_breakthrough()` returned:
- 05395: score=1, reasons=`('importance_boost:+1',)`
- 05339: score=1, reasons=`('importance_boost:+1',)`
- fairwind: score=1, reasons=`('importance_boost:+1',)`
- cursor-self-hosted: score=0, reasons=`('no_breakthrough_signals',)`

All use `format_standard` (Markdown). No `is_breakthrough=True` flag set.

**Dedup check:** 4/4 clean (not in `recently_sent.json` 24h fingerprint window).

**Narrative clustering:** All 4 have `narrative_id=NULL`. `extract_entities_from_item()` finds only `agent` (for 05339) or `benchmark` (for 05395) via title/summary — no entities match existing active/cooling narratives (Jaccard ≥ 0.4 threshold not met). All 4 will attach to NEW narratives on publish. Note: `extract_entities_from_item()` does NOT consult direct `tags` attribute — known gap; tags like `data-sovereignty`, `Кибербезопасность`, `память` are ignored.

**Tier 3 — Below threshold (37 items, QW4-skip):**
- 4× Claude Code releases (2.1.259–2.1.263) — all imp=1
- 7× GitHub Copilot updates (Sept 1–3) — all imp=1
- 5× arxiv (Diffusion TV, CUA-Universe, Design Docs, etc.) — imp=1–2
- 8× TechCrunch general — imp=1–2 (Anthropic settlement, XDOF, rogue agents, etc.)
- 4× techcrunch-ai legacy (Aug 30–31) — imp=1, stale ~9 days, recommend cleanup
- 4× google_ai (Aug AI news, Google Pics, etc.) — imp=1
- 2× justai (caila.io, Selectel partnership) — imp=1, no date
- 3× various (techcrunch ecosystem)

**Critical pipeline issue (recurring):** `run_publish()` calls `list_pending(min_importance=3)` BEFORE `_group_releases_by_source()`. The release-grouper only sees the 4 publishable items, missing the 11 low-imp release items. **Effect:** release-digest feature for Claude Code + GitHub Copilot is effectively dead — they accumulate as imp=1 without consolidation. Suggested fix: relax `min_importance` for `_group_releases_by_source()` to 1 (or even 0) so release patterns can form digests, while keeping `min_importance=3` for standalone items.

**Operational TODOs:**
1. Local OpenClaw cron 21:09 UTC event shows `skipped: 4` without `quiet_deferred` — but current code path returns `quiet_deferred` correctly (verified by dry-run). Either old code was running, OR `--allow-during-quiet` was passed. Worth checking the cron config in OpenClaw.
2. `data/pending_queue.json` is orphan — last touched 2026-09-06 04:05, contains 6 items. Current publisher uses DB only. Could be moved to archive or deleted.
3. `extract_entities_from_item()` ignores `tags` attribute — should be patched to use article-level fields (translated_title, tags, agent_impact) for better clustering.
4. 4× techcrunch-ai items (Aug 30–31, imp=1, age=9d) — past 7d TTL for tier-3 sources, recommend manual cleanup.
5. Last successful TG publish was msg_id=1552 at 12:29 MSK 2026-09-07 (13h ago). Today's cluster run is the 1st since the operator's bot_token/chat_id fix. If 08:05 MSK publish succeeds, validates the infrastructure repair.

## 12:15 MSK — agentsmits-blog-cluster cron run (2nd of day)

**6-hour cycle window:** 12:15 → 18:15 MSK (Tue 2026-09-08).
**Quiet hours:** 23:00–08:00 MSK — **NOT active** (full window is publishing hours).
**Publisher slots available in window:** 12:35, 13:05, 13:35, 14:05, 14:35, 15:05, 15:35, 16:05, 16:35, 17:05, 17:35 (11 slots).

**Pipeline state (re-checked against `data/blog.db`):**
- Pending: **37** items (down from 41 at 00:15 — the 4 Tier 1/2 items from earlier today all published: msg=1553 fairwind at 07:39:58 UTC, msg=1554 05395 at 07:39:58 UTC, msg=1555 cursor at 07:40:09 UTC, msg=1556 05339 at 08:05:08 UTC). 1006 articles now marked `published`.
- Pending by importance: **0** items with imp≥3, 8 with imp=2, 29 with imp=1.
- Pending by source: techcrunch=11, github_copilot=7, arxiv=7, techcrunch-ai=4, claude_code=4, justai=2, google_ai=2.
- Breakthrough items: 0.
- AGI counter (per last publish event): base_days=1460, current_days=1210 (~17% remaining).
- Last successful publish: msg=1556 at 08:05 MSK today (clustered by the 00:15 run).
- `events` table most recent: `publish:20260908T090500` `completed` with `published=0` (queue empty after 08:05 batch); next 08:35, 09:05, 09:35, 09:35+ runs will also be empty.

**Tier classification + slot schedule (12:15 → 18:15 MSK):**

| Tier | Items | Slot (MSK) | Why |
|---|---|---|---|
| **Tier 1** | **0** | — | All 4 Tier-1 items from the 00:15 run already published. No fresh imp≥4 items in queue. |
| **Tier 2** | **0** | — | No fresh imp=3 items in queue. |
| **Tier 3 (QW4-skip)** | **37** | — | All pending items have `importance < 3` → filtered out by `run_publish(min_importance=3)` BEFORE both the standalone loop and `_group_releases_by_source()`. |
| **Idle slots** | 11 of 11 | 12:35–17:35 | Nothing to publish this cycle. |

**Result: zero publishable slots in this 6-hour window.** The publisher cron will run 5/35 slots on every call but each will return `published=0, failed=0, skipped=0` until either: (a) a fresh scanner cycle produces imp≥3 items, or (b) the release-digest feature is fixed (see TODOs).

**Analyst notes (per source group):**

1. **arxiv (7 items, max imp=2)** — All imp≤2 → QW4-skip. Two research themes visible:
   - Embodied/diffusion (Diffusion TV, hybrid GUI+CLI agents CUA-Universe, ViT compression for plant disease) — narrow academic novelty, not breakthrough.
   - LLM evaluation methods (Design Docs as performance analysis tool, LLM behavioral explanations, molecular Déjà Vu search) — method papers, no immediate deployable artifact.
   No candidate for standalone post. Standard format only if importance were boosted. None attached to existing narratives.

2. **techcrunch (11 items, max imp=2)** — Mix of business (XDOF $1.2B Series B, Nscale $3.5B pre-IPO) + AI safety/agent incidents (OpenAI rogue agents, agent publishers vs payment split). Imp=2 caps them below threshold. **Potential narrative n-candidate:** "OpenAI rogue agents" (2 items, Sept 4 — same news cycle about agents escaping lab knowledge). But Jaccard clustering requires entities overlap ≥0.4; `extract_entities_from_item` returns only "agent" (in GENERIC_ENTITIES set → filtered out) and "openai" for both. So they wouldn't merge into a single narrative under current logic. Recommend manual pairing if importance boost desired.

3. **github_copilot (7 items, all imp=1)** — All product/business announcements: model lifecycle (some models sunset), billing (Copilot Business/Enterprise registration), model selection default, content restrictions, budget expiry dates, PR approval workflow, Claude Fable 5.1 availability. None is a release by `_is_release()` regex (which requires `Claude\s*\d+` or similar — "Claude Fable 5.1" matches but 6 others don't because they have no version number). **Per SKILL.md spec** the source ∈ {Claude Code, GitHub Copilot, Cursor} should auto-qualify them as releases — but the implementation in `agentsblog/scoring/impact.py:is_release_item()` does NOT have a source-based whitelist. **Recurring bug.** Even if detected as releases, they wouldn't trigger the digest path because `list_pending(min_importance=3)` filters them out first.

4. **claude_code (4 items, all imp=1)** — Versions 2.1.259–2.1.263 (Sept 7). All version-pattern matches (`2.1.263` etc.) but `_VERSION_RE` only matches `Claude\s*\d+` not `Claude Code\s*\d+` → `_is_release` returns False for all 4. **SKILL.md spec vs implementation mismatch confirmed.** Same digest-blocker as #3.

5. **cursor (0 items)** — Nothing pending. (The `cursor-self-hosted-machines` item from 00:15 was already published at msg=1555.)

6. **google_ai (2 items, max imp=2)** — Aug 2026 AI updates roundup (imp=2, 7d old) + Google Pics (imp=1, 7d old). Both below threshold.

7. **justai (2 items, imp=1, NULL date)** — caila.io launch + Selectel partnership. Date NULL means TTL check undefined — items effectively eternal until manual decision. Recommend adding `scanned_at` fallback for TTL.

8. **techcrunch-ai (4 items, all STALE >7d, imp=1)** — Musk gas turbines, Caterpillar mining AI, US/China drone barriers, Circleback free tier. Ages: 8–9 days. All past the 7-day TTL for tier-3 sources. **Recommend stale-cleanup** (drop from pending without publishing).

**Breakthrough check:** 0/37. All items fail ARCHITECTURE_PATTERNS or SOTA detection (they are routine releases, business news, or pure research).

**Narrative clustering:** All 37 items have `narrative_id=NULL`. `extract_entities_from_item()` returns mostly generic entities (agent, agents, open source, benchmark) which are filtered out by `_GENERIC_ENTITIES` set. Without those, no Jaccard overlap reaches 0.4 threshold with existing narratives. All would attach to NEW single-item narratives on publish.

**Dedup check:** Clean — no overlaps with `recently_sent.json` 24h fingerprint window (last fingerprints are from 2026-09-06 04:05).

**Critical pipeline issue (still recurring from 00:15):**
- `run_publish()` calls `list_pending(min_importance=3)` BEFORE `_group_releases_by_source()`. The release-grouper never sees the 11 low-imp release items (4× Claude Code, 7× GitHub Copilot).
- **Effect:** release-digest feature is effectively dead — Claude Code + GitHub Copilot items accumulate as imp=1 without ever being consolidated.
- **Suggested fix (Option A — minimal):** in `agentsblog/publishing/publisher.py:run_publish()`, after the `min_importance` filter for the standalone loop, call `_group_releases_by_source()` on a separate query: `list_pending(conn, min_importance=1)` and only emit digests where the source has ≥2 release items, without affecting the standalone-cap (max_per_source_per_run).
- **Option B:** relax the importance floor for sources that are auto-release (Claude Code, GitHub Copilot, Cursor) to imp=1, so standalone posts from those sources can also publish. Likely noisier, but matches SKILL.md spec ("Source ∈ {Claude Code, GitHub Copilot, Cursor}" auto-triggers release treatment).

**Operational TODOs (updated):**
1. Pipeline bug — release digest disabled by `min_importance=3` filter. **Recommended fix:** patch `run_publish()` to query release items separately from high-importance standalone items (Option A above). Without this, GitHub Copilot and Claude Code churn stays imp=1 forever.
2. 4× techcrunch-ai items (Aug 30–31, 8–9d old) — past 7d TTL. Recommend one-shot `python -m agentsblog cleanup --source techcrunch-ai --older-than 7d` at next manual run.
3. 2× justai items with NULL date — TTL undefined. Recommend either backfilling `scanned_at` or adding date-as-now fallback.
4. Implementation gap: `is_release_item()` should accept source whitelist (Claude Code, GitHub Copilot, Cursor) per SKILL.md spec, not just regex pattern. Adding this alone won't unblock the digests (the importance filter is the bigger blocker) but it makes the heuristics internally consistent.
5. Fresh scanner cycle produced `new_pending=0` for all sources at 08:30 and 09:00 UTC today — either upstream feeds are quiet or scanner is stuck. Worth checking source_health table on next manual run.

## 18:15 MSK — agentsmits-blog-cluster cron run (3rd of day)

**6-hour cycle window:** 18:15 → 00:15 MSK (Tue 2026-09-08 → Wed 2026-09-09).
**Quiet hours:** 23:00–08:00 MSK — **PARTIAL** (covers 23:00–00:15 of the window).
**Publisher slots available (active):** 18:35, 19:05, 19:35, 20:05, 20:35, 21:05, 21:35, 22:05, 22:35 (9 slots before quiet starts).
**Publisher slots (quiet-deferred):** 23:05, 23:35, 00:05 (3 slots within quiet window).

**Pipeline state (re-checked against `data/blog.db`):**
- Pending: **38** items (up from 37 at 12:15 — one new: `techcrunch-mistral-raises-e3b-...`, imp=2, added 14:30 UTC = 17:30 MSK)
- Pending by importance: **0** items with imp≥3, 9 with imp=2, 29 with imp=1
- Pending by source: techcrunch=12 (was 11), github_copilot=7, arxiv=7, techcrunch-ai=4, claude_code=4, justai=2, google_ai=2
- Breakthrough items: 0
- AGI counter: base_days=1460, current_days=1210 (~17% remaining)
- Last successful publish: msg=1562 (`huggingface-safety-for-whom`) at 14:35 MSK today. Subsequent runs (15:05, 15:35, 16:05, 16:35, 17:05, 17:35, 18:05) all returned `published=0` — queue is empty of high-imp items.
- Events table last entry: event 184 at 15:16:38 UTC (a manual dry-run probe I executed).

**Tier classification + slot schedule (18:15 → 00:15 MSK):**

| Tier | Items | Slot (MSK) | Why |
|---|---|---|---|
| **Tier 1** | **0** | — | No pending items with `importance ≥ 3`. The 3 imp≥3 items that were queued at 12:15 were all published between 12:15 and 14:35 (`openai-the-work-now-within-reach`, `openai-supporting-journalism-from-classrooms-to-newsrooms`, `huggingface-safety-for-whom`). |
| **Tier 2** | **0** | — | No pending items with `importance == 3`. |
| **Tier 3 (QW4-skip)** | **38** | — | All pending items have `importance < 3` → filtered out by `run_publish(min_importance=3)` BEFORE both the standalone loop and `_group_releases_by_source()`. |
| **Idle slots (active)** | 9 of 9 | 18:35–22:35 | Nothing to publish this cycle. Each call will return `published=0, failed=0, skipped=0`. |
| **Quiet-deferred slots** | 3 of 3 | 23:05, 23:35, 00:05 | Each call will return `quiet_deferred=38, published=0`. |

**Result: zero publishable slots in this 6-hour window.** The publisher cron will fire on every slot but each will return no-op until either (a) a fresh scanner cycle produces imp≥3 items, or (b) the release-digest feature is unblocked.

**Analyst notes (per source group, Tier 3):**

1. **techcrunch (12 items, max imp=2)** — 9 imp=1 + 3 new imp=2 (Mistral €3B sovereign AI from 17:30 MSK is the freshest). The Mistral item is geographically/economically significant but capped at imp=2 by the heuristic (no architecture/sota keyword hit). Other 2: Authors vs Anthropic settlement (Sept 6, ongoing litigation theme); XDOF $1.2B Series B (Sept 4). Two related "rogue agents" items (Sept 4) form a thematic pair but can't merge because `extract_entities_from_item()` filters out the "agent"/"openai" tokens as generic. **Potential Tier 2 narrative n-candidate:** "OpenAI rogue agents" (2 items). Manual override would require boosting importance to ≥3.

2. **arxiv (7 items, max imp=2)** — 4 imp=1 + 3 imp=2. Themes: embodied/diffusion (Diffusion TV), GUI/CLI hybrid agents (CUA-Universe), LLM evaluation methods (Design Docs), ViT compression. None is breakthrough (no architecture/sota/open-source signal). Stale — `arxiv-2609.05404` and `arxiv-2609.05381` and `arxiv-2609.05374` ALL FAILED analysis (per `cron-analyze.log` showing `unbalanced JSON` and `[WinError 10054]` errors from Ollama). They have empty `summary`/`agent_impact`/`business_impact`/`it_impact` → even if importance were boosted, they would be skipped with `no analysis`.

3. **github_copilot (7 items, all imp=1)** — Product/business updates: budget expiry, code review PR approval, Claude Fable 5.1 GA, default model selection, content exclusions, model deprecation, business/enterprise signup reopening. None matches `_is_release()` regex (`\bClaude\s*\d+` fails because "Claude Code 2.1.263" has "Code" between the model name and digits). Same digest-blocker as previous runs. SKILL.md spec says `Source ∈ {Claude Code, GitHub Copilot, Cursor}` should auto-trigger release grouping — implementation gap.

4. **claude_code (4 items, all imp=1)** — Versions 2.1.259, 2.1.260, 2.1.261, 2.1.263 (Sept 7). All version-pattern matches conceptually but `_VERSION_RE = \b(?:GPT|Claude|...)[\s\-]*\d+(?:\.\d+)?\b` does NOT match "Claude Code 2.1.263" because the regex needs digits immediately after Claude. **Verified by direct test**: all 4 titles return `False` from `_VERSION_RE.search(t)`. This is the root cause of the digest-blocking bug.

5. **techcrunch-ai (4 items, all imp=1, STALE >7d)** — Aug 30–31 items, ages 8–9 days. Past 7-day TTL for tier-3 sources. Still pending because cleanup cron isn't running. **Recommend manual cleanup on next manual session.**

6. **google_ai (2 items)** — Aug 2026 AI roundup (imp=2, 7d old) + Google Pics (imp=1, 7d old). Both at TTL boundary for tier-3 source.

7. **justai (2 items, imp=1, NULL date)** — caila.io launch + Selectel partnership. Date NULL means TTL check undefined. Already flagged in 12:15 run — needs manual decision.

**Breakthrough check:** 0/38. Confirmed by `compute_agi()` returning 1210/17% and zero `is_breakthrough=True` flags in the DB.

**Narrative clustering:** All 38 items have `narrative_id=NULL`. The 2 "OpenAI rogue agents" techcrunch items are thematically related but won't auto-merge via Jaccard (generic entity filter). All would attach to NEW single-item narratives on publish.

**Dedup check:** Clean — no overlaps with `recently_sent.json` 24h fingerprint window.

**Operational anomalies:**

1. **Cron gap detected.** `events` table last entry at 15:16:38 UTC (was my dry-run probe). No events at 15:35, 16:00, 16:05, 16:30, 16:35, 17:00, 17:05, 17:30, 17:35, 18:00, 18:05 UTC — **9 expected publisher/scan slots missing**. Possible causes: (a) cron scheduler paused or failed silently, (b) cron is configured but not firing on Windows Task Scheduler, (c) a previous failure caused backoff that hasn't expired. **Worth checking Windows Task Scheduler status** for `agentsblog-publish` and `agentsblog-scan` jobs at next manual session. The actual content pipeline is healthy (last scan at 15:00 found 0 new items, last real publish at 14:35 was successful). The cron just isn't firing on schedule.

2. **Tier 3 source tier classification**: `techcrunch` is registered as tier 3 in `sources` table (penalty 0.85×), not tier 2 as some items suggest. Items from techcrunch need an importance boost of ~+1 to clear the min_publish_importance=3 floor. Current scoring caps them at imp=2.

3. **The Mistral item** (`techcrunch-mistral-raises-e3b-...`) is the freshest pending item (1h old). Mistral is registered as tier 1 source but the article has importance=2. If a fresh tier-1-source article with sovereign-AI angle is newsworthy enough to boost manually, this is a candidate — but it's standard business-funding news, no architecture/sota, so no automatic boost.

**Operational TODOs (carried forward + new):**
1. Pipeline bug — release digest disabled by `min_importance=3` filter AND `_is_release()` regex doesn't match Claude Code. **Recommended combined fix:** (a) extend `_VERSION_RE` to allow optional model-suffix words (e.g. `Claude(?:\s+Code)?\s*\d+`), OR (b) add source whitelist to `is_release_item()` per SKILL.md spec, AND (c) call `_group_releases_by_source()` on `list_pending(conn, min_importance=1)` for release-type items separately. Without these fixes, GitHub Copilot + Claude Code churn stays imp=1 forever.
2. Cron scheduler appears to have a 3-hour gap (no events between 15:16 and 18:15 UTC today). **Recommended fix:** inspect Windows Task Scheduler for `agentsblog-publish` and `agentsblog-scan` jobs at next manual session.
3. 4× techcrunch-ai items (Aug 30–31, 8–9d old) — past 7d TTL. Recommend one-shot cleanup.
4. 2× justai items with NULL date — TTL undefined. Recommend either backfilling `date` or adding date-as-now fallback.
5. arxiv items with FAILED analysis (`arxiv-2609.05404`, `arxiv-2609.05381`, `arxiv-2609.05374`) — even if importance were boosted, they'd be skipped with `no analysis`. Need Ollama retry pass to fill in `summary`/`agent_impact`/`business_impact`/`it_impact`.
6. Fresh scanner cycle has produced `new_pending=0` for 4 consecutive runs (12:00, 13:00, 14:00, 15:00 UTC). Upstream feeds (anthropic, openai, deepmind, mistral, etc.) are returning empty/dup-only results. Either the upstream RSS feeds are quiet, or the scanner is failing to extract new content. Worth checking `source_health` table for last-success timestamps.
