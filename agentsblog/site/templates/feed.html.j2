{% extends "base.html.j2" %}
{% from "_macros.html.j2" import post_card, archive_card, subscribe_banner %}

{% block content %}
  <section class="editorial-intro">
    <span class="eyebrow">Авторский взгляд на прикладной ИИ</span>
    <h1>Что изменилось.<br><em>И что с этим делать.</em></h1>
    <p>Разбираю модели, инструменты и агентные системы: как они устроены,
    где полезны и какие ограничения скрываются за громким анонсом.</p>
    <div class="intro-topics"><span>Агенты</span><span>Инженерия</span><span>Практика</span></div>
  </section>
  {% if active is not defined %}{% set active = 'feed' %}{% endif %}

  {{ subscribe_banner() }}

  <section class="section">
    <div class="section-label">Последние материалы</div>
    <div class="section-header">
      <h2 class="section-title">Актуальные новости</h2>
      <span class="section-count">{{ feed|length }} публикаций</span>
    </div>

    {% if feed %}
      <div class="feed-grid">
        {% for post in feed %}
          {{ post_card(post) }}
        {% endfor %}
      </div>
    {% else %}
      <div class="empty-state">
        <div class="empty-state-icon">&#128236;</div>
        <p>Постов этой недели пока нет</p>
      </div>
    {% endif %}
  </section>

  {% if archive %}
  <section class="section">
    <div class="section-label">Архив</div>
    <div class="section-header">
      <h2 class="section-title">Статьи прошлых недель</h2>
      <span class="section-count">{{ archive|length }} статей</span>
    </div>
    <div class="archive-grid">
      {% for post in archive %}
        {{ archive_card(post) }}
      {% endfor %}
    </div>
  </section>
  {% endif %}
{% endblock %}
